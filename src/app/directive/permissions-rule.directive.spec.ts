import { PermissionsRuleDirective } from './permissions-rule.directive';

describe('PermissionsRuleDirective', () => {
  it('should create an instance', () => {
    const directive = new PermissionsRuleDirective();
    expect(directive).toBeTruthy();
  });
});
